<!DOCTYPE html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">
    <head>
       <title>cars project</title>
    </head>
    <body class="antialiased">
        <h2>Welcome {{$name}}</h2>
        <h5>my age is {{$age}}</h5>
    </body>
</html>
